#pragma once
#include <Windows.h>

const int SIZE_X = 30;
const int SIZE_Y = 30;

const int SCREEN_X = 30;
const int SCREEN_Y = 30;

const int screenWidth = GetSystemMetrics(SM_CXSCREEN);;
const int screenHeight = GetSystemMetrics(SM_CYSCREEN);